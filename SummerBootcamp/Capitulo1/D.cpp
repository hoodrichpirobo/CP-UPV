#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio;
  cin.tie(nullptr);

  double a, b, c;
  string d;
  cin >> a >> b >> c >> d;
  cout << ceil(a) << '\n';
  cout << floor(b) << '\n';
  cout << round(c) << '\n';
  cout << d << '\n';

  return 0;
}
