#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  double y;
  cin >> y;
  cout << setprecision(15) << fixed << log(y) << endl;

  return 0;
}
