#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  ll y;
  cin >> y;
  cout << setprecision(5) << fixed << log2(y) << endl;

  return 0;
}
