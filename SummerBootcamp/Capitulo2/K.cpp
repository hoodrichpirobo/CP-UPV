#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  ll A;
  cin >> A;

  cout << setprecision(5) << fixed << sqrt(A) << endl;

  return 0;
}
