#include <bits/stdc++.h>
using namespace std;
using ll = unsigned long long;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  ll n, k;
  cin >> n >> k;
  cout << (n << k) << endl;
  cout << (n >> k) << endl;

  return 0;
}
